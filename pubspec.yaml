name: farmlens_app
description: FarmLens Edge AI crop health monitoring companion app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.0
  go_router: ^13.0.0
  http: ^1.2.0
  fl_chart: ^0.68.0
  shared_preferences: ^2.2.0
  intl: ^0.19.0
  flutter_local_notifications: ^17.0.0
  share_plus: ^7.0.0
  path_provider: ^2.1.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true